function hello(nama: string, umur: number): string{
    return `Selamat Datang ${nama} di PWL, umur ${nama} adalah ${umur} tahun`;
}

const mahasiswa = "Aisyah";
const umur:number = 17;

console.log(hello(mahasiswa, umur));